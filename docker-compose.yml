services:
  # ----------------------------------------------------------------------------
  # GigaHub Mobile (React SPA servida por Nginx Alpine para Capacitor Live Remote)
  # ----------------------------------------------------------------------------
  gigahub-mobile:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gigahub-mobile
    restart: unless-stopped
    networks:
      - gigahub-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=gigahub-network"
      - "traefik.http.routers.mobile.entrypoints=websecure"
      - "traefik.http.routers.mobile.rule=Host(`mobile.gigahub.site`)"
      - "traefik.http.routers.mobile.tls=true"
      - "traefik.http.routers.mobile.tls.certresolver=letsencrypt"
      - "traefik.http.services.mobile.loadbalancer.server.port=80"

networks:
  gigahub-network:
    external: true
